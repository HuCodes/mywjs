# editorconfig.org          为了在不同的ide中展示的效果一样的项目的配置文件
root-true
[*] #通配符，表示以下配置应用到整个项目中
charset=utf-8
indent_size=2   #缩进大小为2
indent_style=space  #缩进样式为空格
trim_trailing_whitespace=true   #去除多余的空格


[*.md]
trim_trailing_whitespace=false